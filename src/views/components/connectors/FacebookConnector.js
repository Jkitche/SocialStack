import { connect } from 'react-redux';
import FacebookController from '../FacebookController';

const ConnectedFacebookController = connect(
	state => ({

	}),
	dispatch => ({

	})
)(FacebookController);

export default ConnectedFacebookController;
